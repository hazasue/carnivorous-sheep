using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Leap_control : MonoBehaviour
{
    public int Leap_count;
    // Start is called before the first frame update
    void Start()
    {
        Leap_count = 0;

    }

    // Update is called once per frame
    void Update()
    {

    }

}
